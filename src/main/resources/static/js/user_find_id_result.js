/*$(document).ready(function() {
  $('#user_id').on('input', function() {
    var val = $(this).val();
    if (val.length >= 5) {
      $(this).val(val.substring(0, 2) + '***' + val.substring(5));
    } else if (val.length > 2) {
      $(this).val(val.substring(0, 1) + '**' + val.substring(4));
    }
  });
});*/


